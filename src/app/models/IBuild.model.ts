export interface IBuild {
  tipoBuild: string;
  imagem: string;
  pagina?: string;
}
export interface IBuilds{
  tipoBuild: string;
  nome: string;
  imagem: string;
  discrucao: string;
  pagina?: string;
}
